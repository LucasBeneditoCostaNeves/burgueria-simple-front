import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    margin-top: 25px;
    align-items: baseline;

  h1 {
    font-size: 30px;
    font-weight: 700;
    margin-right: 5px;
    color: var(--cinza-600);
  }

  .pink {
    color: var(--vermelho);
    font-size: 20px;
    font-weight: 600;
  }
`;
